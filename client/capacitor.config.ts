import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.reserveit.app',
  appName: 'ReserveIt',
  webDir: 'dist/client/browser'
};

export default config;
