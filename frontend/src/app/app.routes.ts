import { Routes } from '@angular/router';
import { HomeMapPageComponent } from './components/home-map-page.component';

export const routes: Routes = [
  { path: '', component: HomeMapPageComponent }
];
